# SalonFlow Adapter

The SalonFlow adapter is an authenticated HTTP client that targets a configurable API base URL.

Use it for:

- Demo tenant integrations
- Staging bookings
- Production integrations once approved

It is intentionally not coupled to SalonFlow database models.

